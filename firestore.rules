rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{project} {
      allow read: if true;  // Allow anyone to read projects
      allow write: if false; // Keep write restricted
    }
    
    match /{document=**} {
      allow read, write: if false; // Keep other collections restricted
    }
  }
}